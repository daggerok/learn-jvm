server:
  port: 8003
spring:
  application:
    name: spring-boot-kotlin-gradle-webapp
  jpa:
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:hsqldb:mem:spring-boot-kotlin-gradle-webapp-db
  data:
    rest:
      base-path: /api/v1
  mustache:
    suffix: .hbs
management:
  context-path: /admin
