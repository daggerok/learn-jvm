server:
  port: 8002
spring:
  application:
    name: spring-boot-java-gradle-webapp
  jpa:
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:hsqldb:mem:spring-boot-java-gradle-webapp-db
  data:
    rest:
      base-path: /api/v1
  mustache:
    suffix: .hbs
management:
  context-path: /admin
