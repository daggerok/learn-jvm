server:
  port: 8001
spring:
  application:
    name: spring-boot-groovy-gradle-webapp
  jpa:
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:hsqldb:mem:spring-boot-groovy-gradle-webapp-db
  data:
    rest:
      base-path: /api/v1
  mustache:
    suffix: .hbs
management:
  context-path: /admin
